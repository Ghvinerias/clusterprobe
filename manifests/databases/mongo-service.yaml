apiVersion: v1
kind: Service
metadata:
  name: clusterprobe-mongo
  labels:
    app.kubernetes.io/name: clusterprobe
    app.kubernetes.io/component: mongo
    app.kubernetes.io/part-of: clusterprobe
    app.kubernetes.io/managed-by: kustomize
spec:
  type: ClusterIP
  ports:
    - name: mongo
      port: 27017
      targetPort: 27017
  selector:
    app.kubernetes.io/name: clusterprobe
    app.kubernetes.io/component: mongo
