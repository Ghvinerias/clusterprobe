run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - gosec
    - goimports
    - exhaustive
    - wrapcheck
    - lll

linters-settings:
  lll:
    line-length: 120
    tab-width: 1
